#include<stdio.h>

void main(int argc,char* argv[]) {	//array "[]" and pointer "*" is internally same we can also use "int argc,char** argv"

	printf("%d\n",argc);		//prints the length of the command line argument
}
